sources:
  - name: raw_data
    schema: raw_data_my_cases
    tables:
      - name: raw_customers
        #tests: #somehow this doesnt work in the dbt_studio but works in dbt core.
        #  - not_null:
        #    column_name: id
        #    description: "Primary Key of the source"
        columns:
          - name: id 
            tests:
              - not_null
            description: Primary key of the table
      - name: raw_orders
        description: '{{ doc("src_dbt_studio_") }}'
        freshness:
          warn_after:
            count: 5
            period: day
        loaded_at_field: ordered_at